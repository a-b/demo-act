name: Demo Vars

on:
  workflow_dispatch:

jobs:
  demo-vars:
    runs-on: ubuntu-latest
    steps:
      
      - name: Print message
        run: |
          echo DEMO FOO: ${{ vars.FOO }}
          echo DEMO BAR: ${{ vars.BAR }}
