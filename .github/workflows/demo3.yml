name: Demo Local Worker

on:
  workflow_dispatch:

jobs:
  demo-local-worker:
    runs-on: self-hosted
    steps:
      
      - name: Print message
        run: |
          echo DEMO FOO: ${{ vars.FOO }}
          echo DEMO BAR: ${{ vars.BAR }}
